# Login / Register Button Display Fix

本补丁只修改 `index.html`。

修复内容：

- 用户未登录时：右上角显示「登录」「注册」。
- 用户登录后：右上角隐藏「登录」「注册」，显示「进入会员中心」「退出」。
- 手机端同样处理：登录后不再显示登录入口，改为显示「进入会员中心」。
- 不修改数据库、不修改新闻接口、不影响注册、登录、验证码、会员资料、新闻板块。

上传方式：

把本包里的 `index.html` 覆盖到 GitHub 仓库根目录，然后提交部署即可。
